 // We will use the "unit" variable in order to get the symbol

import type { Unit } from "../types/unit";

 export const unitSymbols: Record<Unit, string> = {
    celsius: "°C",
    kelvin: "K",
    fahrenheit: "F",
  };